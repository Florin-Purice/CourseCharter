<UserControl x:Class="WoTMapWPF.CustomControls.LineStipple"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:System="clr-namespace:System;assembly=mscorlib"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WoTMapWPF.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="Grid" x:Key="BitStyle">
            <Setter Property="Height" Value="{Binding Path=BitSize, RelativeSource={RelativeSource AncestorType=local:LineStipple}}"/>
            <Setter Property="Width" Value="{Binding Path=Height, RelativeSource={RelativeSource Self}}"/>
            <Setter Property="Background" Value="{Binding Path=ColorInactive, RelativeSource={RelativeSource AncestorType=local:LineStipple}}"/>
            <Style.Triggers>
                <Trigger Property="Tag">
                    <Trigger.Value>
                        <System:Boolean>True</System:Boolean>
                    </Trigger.Value>
                    <Setter Property="Background" Value="{Binding Path=ColorActive, RelativeSource={RelativeSource AncestorType=local:LineStipple}}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid Name="BitContainer">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
    </Grid>
</UserControl>
